package com.battaglianavale.Server;

public enum TipoMessaggio {
    JOIN,
    JOIN_OK,
    GAME_START,
    PLACE_SHIPS,
    PLACE_SHIPS_OK,
    ATTACK,
    ATTACK_RESULT,
    INCOMING_ATTACK,
    TURN_CHANGE,
    ERROR,
    GAME_OVER

}
